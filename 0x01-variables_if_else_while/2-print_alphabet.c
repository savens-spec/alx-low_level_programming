#include <stdio.h>
/**
 * main - print if the number is positive, zero, negetive
 *
 * Description: using the main function
 * this program prints "Programing is positive, zero, negetive
 * Return (0)
 */
int main(void)
{
	char ch;
	for (ch = 'a' ; ch <= 'z' ; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
